# available modes: development, production
mode: development

server:
  port: 8888

database:
  user: l2sh
  name: admissions
  host: database
  port: 5432
  connection:
    timezone: Europe/Moscow

cache:
  host: cache
  port: 6379

logging:
  level: debug

jwt:
  access_lifetime: 15m
  refresh_lifetime: 720h
